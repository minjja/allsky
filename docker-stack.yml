version: '3.7'

services:

    app:
        image: ghcr.io/minjja/allsky-website:latest
        ports:
            - 80:80/tcp
        configs:
            - source: config.js
              target: /var/www/html/config.js

configs:
    config.js:
        external: true
